--文字在左，图片在右
from tkinter import *

root = Tk()
root.title("标签学习")
textLab = Label(root,text = "这是一个标签")
textLab.pack(side = LEFT)
pic = PhotoImage(file = "1.PNG")
picLab = Label(root,image = pic)
picLab.pack(side = RIGHT)
mainloop()
